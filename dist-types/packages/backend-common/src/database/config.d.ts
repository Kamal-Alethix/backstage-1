/**
 * Merges database objects together
 *
 * @public
 * @param config - The base config. The input is not modified
 * @param overrides - Any additional overrides
 */
export declare function mergeDatabaseConfig(config: any, ...overrides: any[]): any;
