import { Transformer } from '../transformer';
/**
 * Returns a transformer that inserts all style rules into the given element's head tag.
 */
export declare const useStylesTransformer: () => Transformer;
