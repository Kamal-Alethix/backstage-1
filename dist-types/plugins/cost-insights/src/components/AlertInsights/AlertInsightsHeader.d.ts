/// <reference types="react" />
declare type AlertInsightsHeaderProps = {
    title: string;
    subtitle: string;
};
export declare const AlertInsightsHeader: ({ title, subtitle, }: AlertInsightsHeaderProps) => JSX.Element;
export {};
