/// <reference types="react" />
import { Alert } from '../../types';
declare type AlertInsightsSectionHeaderProps = {
    alert: Alert;
    number: number;
};
export declare const AlertInsightsSectionHeader: ({ alert, number, }: AlertInsightsSectionHeaderProps) => JSX.Element;
export {};
