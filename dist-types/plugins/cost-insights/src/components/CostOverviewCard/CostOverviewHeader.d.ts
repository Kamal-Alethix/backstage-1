import { PropsWithChildren } from 'react';
declare type CostOverviewHeaderProps = {
    title: string;
    subtitle?: string;
};
export declare const CostOverviewHeader: ({ title, subtitle, children, }: PropsWithChildren<CostOverviewHeaderProps>) => JSX.Element;
export {};
