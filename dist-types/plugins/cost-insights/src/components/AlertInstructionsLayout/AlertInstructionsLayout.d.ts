import { PropsWithChildren } from 'react';
declare type AlertInstructionsLayoutProps = {
    title: string;
};
export declare const AlertInstructionsLayout: ({ title, children, }: PropsWithChildren<AlertInstructionsLayoutProps>) => JSX.Element;
export {};
