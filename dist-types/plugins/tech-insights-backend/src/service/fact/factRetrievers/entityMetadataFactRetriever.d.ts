import { FactRetriever } from '@backstage/plugin-tech-insights-node';
/**
 * Generates facts which indicate the completeness of entity metadata.
 *
 * @public
 */
export declare const entityMetadataFactRetriever: FactRetriever;
