/// <reference types="react" />
declare type Props = {
    guid: string;
    name: string;
    permalink: string;
    duration: number;
};
export declare const DashboardSnapshot: ({ guid, name, permalink, duration, }: Props) => JSX.Element;
export {};
