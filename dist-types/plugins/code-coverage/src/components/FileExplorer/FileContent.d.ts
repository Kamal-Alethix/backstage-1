/// <reference types="react" />
import { FileEntry } from '../../types';
declare type Props = {
    filename: string;
    coverage: FileEntry;
};
export declare const FileContent: ({ filename, coverage }: Props) => JSX.Element;
export {};
