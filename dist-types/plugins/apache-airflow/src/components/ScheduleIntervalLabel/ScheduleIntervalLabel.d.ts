/// <reference types="react" />
import { ScheduleInterval } from '../../api/types';
interface Props {
    interval: ScheduleInterval | undefined;
}
export declare const ScheduleIntervalLabel: ({ interval }: Props) => JSX.Element;
export {};
