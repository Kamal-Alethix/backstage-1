/// <reference types="react" />
import { Status } from '../../types';
declare type Props = {
    status: Status;
    styles?: string;
};
export declare const StatusTag: ({ status, styles }: Props) => JSX.Element;
export {};
