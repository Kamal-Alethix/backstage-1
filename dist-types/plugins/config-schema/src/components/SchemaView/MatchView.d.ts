/// <reference types="react" />
import { Schema } from 'jsonschema';
export declare function MatchView({ path, depth, schema, label, }: {
    path: string;
    depth: number;
    schema: Schema[];
    label: string;
}): JSX.Element;
