/// <reference types="react" />
import { PullRequestGroup } from '../types';
declare type PullRequestGridProps = {
    pullRequestGroups: PullRequestGroup[];
};
export declare const PullRequestGrid: ({ pullRequestGroups, }: PullRequestGridProps) => JSX.Element;
export {};
