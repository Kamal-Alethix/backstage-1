/// <reference types="react" />
import { DomainEntity } from '@backstage/catalog-model';
declare type DomainCardProps = {
    entity: DomainEntity;
};
export declare const DomainCard: ({ entity }: DomainCardProps) => JSX.Element;
export {};
