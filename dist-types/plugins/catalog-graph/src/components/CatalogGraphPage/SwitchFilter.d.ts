/// <reference types="react" />
export declare type Props = {
    label: string;
    value: boolean;
    onChange: (value: boolean) => void;
};
export declare const SwitchFilter: ({ label, value, onChange }: Props) => JSX.Element;
