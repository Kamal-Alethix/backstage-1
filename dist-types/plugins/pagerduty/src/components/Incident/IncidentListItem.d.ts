/// <reference types="react" />
import { Incident } from '../types';
declare type Props = {
    incident: Incident;
};
export declare const IncidentListItem: ({ incident }: Props) => JSX.Element;
export {};
