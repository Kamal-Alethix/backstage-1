/// <reference types="react" />
declare type Props = {
    serviceId: string;
    refreshIncidents: boolean;
};
export declare const Incidents: ({ serviceId, refreshIncidents }: Props) => JSX.Element;
export {};
