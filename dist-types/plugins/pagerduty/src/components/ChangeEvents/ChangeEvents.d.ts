/// <reference types="react" />
declare type Props = {
    serviceId: string;
    refreshEvents: boolean;
};
export declare const ChangeEvents: ({ serviceId, refreshEvents }: Props) => JSX.Element;
export {};
